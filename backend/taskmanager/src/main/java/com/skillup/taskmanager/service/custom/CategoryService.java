package com.skillup.taskmanager.service.custom;

import com.skillup.taskmanager.dto.CategoryDto;
import com.skillup.taskmanager.service.base.BaseService;

public interface CategoryService extends BaseService<CategoryDto, Long> {
    // No custom methods yet, but can be added later
}
